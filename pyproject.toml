[project]
name = "ai-lab"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Carlos Mata", email = "96638258+AMATEURBOSS@users.noreply.github.com" }
]
requires-python = ">=3.14"
dependencies = [
    "ollama>=0.6.2",
]

[project.scripts]
ai-lab = "ai_lab:main"

[build-system]
requires = ["uv_build>=0.12.18,<0.13.0"]
build-backend = "uv_build"
